diff --git a/backend/models/attempt.py b/backend/models/attempt.py
index 58caffb..b643bfc 100644
--- a/backend/models/attempt.py
+++ b/backend/models/attempt.py
@@ -16,7 +16,7 @@ from backend.database import Base
 class Participant(Base):
     """
     SSOT 6.11 (attempt.py):
-    participants: id, nome, sobrenome, email, tipo_aplicacao
+    participants: id, nome, sobrenome, email, empresa_nome, tipo_aplicacao
     """
     __tablename__ = "participants"
 
@@ -25,9 +25,11 @@ class Participant(Base):
     nome: Mapped[str] = mapped_column(String(255), nullable=False)
     sobrenome: Mapped[Optional[str]] = mapped_column(String(255), nullable=True)
     email: Mapped[Optional[str]] = mapped_column(String(255), nullable=True)
+    empresa_nome: Mapped[Optional[str]] = mapped_column(String(255), nullable=True)
 
     # SSOT: tipo_aplicacao (ex.: "pessoal" | "empresa") — manter como string e validar via schema/serviço
     tipo_aplicacao: Mapped[str] = mapped_column(String(32), nullable=False)
+
     attempts: Mapped[List["Attempt"]] = relationship(
         back_populates="participant",
         cascade="all, delete-orphan",
@@ -35,8 +37,6 @@ class Participant(Base):
     )
 
 
-
-
 class Attempt(Base):
     """
     SSOT 6.11 (attempt.py):
diff --git a/backend/repositories/attempt.py b/backend/repositories/attempt.py
index 1423fee..faf7ea4 100644
--- a/backend/repositories/attempt.py
+++ b/backend/repositories/attempt.py
@@ -1,62 +1,71 @@
 from __future__ import annotations
 
-from typing import Optional, Sequence
+from typing import Optional
 
 from sqlalchemy import func, select
 from sqlalchemy.ext.asyncio import AsyncSession
 
 from backend.models.attempt import Answer, Attempt, Participant
-from backend.repositories import cliente as repo_cliente
-from backend.services.token import token_hash
 
 
-# --- attempt lookup ---
-
-async def get_attempt_by_token_hash(session: AsyncSession, token_hash_hex: str) -> Optional[Attempt]:
-    res = await session.execute(select(Attempt).where(Attempt.token_hash == token_hash_hex))
-    return res.scalar_one_or_none()
-
-
-async def get_attempt_by_token(session: AsyncSession, token: str) -> Optional[Attempt]:
-    return await get_attempt_by_token_hash(session, token_hash(token))
-
-
-# --- create (bootstrap seed enquanto admin não existe) ---
-
 async def create_participant_and_attempt_default_seed(
     session: AsyncSession,
     *,
     nome: str,
     sobrenome: Optional[str],
     email: Optional[str],
+    empresa_nome: Optional[str] = None,
     tipo_aplicacao: str,
     token_hash_hex: str,
     testdef_version: str = "v1",
 ) -> Attempt:
     """
-    Cria Participant + Attempt usando o seed DEFAULT_* (Cliente/Rodada/Setor).
-    SSOT: fluxo público atual ainda usa bootstrap seed (até admin existir).
+    Cria Participant + Attempt usando os DEFAULT_* do ambiente atual.
     """
-    cli = await repo_cliente.get_default_cliente(session)
-    rd = await repo_cliente.get_default_rodada(session)
-    st = await repo_cliente.get_default_setor(session)
-
     p = Participant(
         nome=nome,
         sobrenome=sobrenome,
         email=email,
+        empresa_nome=empresa_nome,
         tipo_aplicacao=tipo_aplicacao,
     )
     session.add(p)
     await session.flush()
 
+    # Bootstrap atual do MVP: usa 1 cliente/rodada/setor seeded.
+    # Mantido como no runtime atual.
+    from backend.models.cliente import Cliente, RodadaAplicacao, SetorEmpresa
+
+    cliente = (
+        await session.execute(
+            select(Cliente).order_by(Cliente.criado_em.asc()).limit(1)
+        )
+    ).scalar_one()
+
+    rodada = (
+        await session.execute(
+            select(RodadaAplicacao)
+            .where(RodadaAplicacao.cliente_id == cliente.id)
+            .order_by(RodadaAplicacao.data_inicio.asc())
+            .limit(1)
+        )
+    ).scalar_one()
+
+    setor = (
+        await session.execute(
+            select(SetorEmpresa)
+            .where(SetorEmpresa.cliente_id == cliente.id)
+            .order_by(SetorEmpresa.nome.asc())
+            .limit(1)
+        )
+    ).scalar_one_or_none()
+
     attempt = Attempt(
-        invite_id=None,
         token_hash=token_hash_hex,
         participant_id=p.id,
-        cliente_id=cli.id,
-        rodada_id=rd.id,
-        setor_id=st.id,
+        cliente_id=cliente.id,
+        rodada_id=rodada.id,
+        setor_id=setor.id if setor else None,
         cargo=None,
         status="IN_PROGRESS",
         progress=0,
@@ -67,23 +76,27 @@ async def create_participant_and_attempt_default_seed(
     return attempt
 
 
-# --- answers (SSOT) ---
+async def get_attempt_by_token_hash(session: AsyncSession, token_hash_hex: str) -> Optional[Attempt]:
+    res = await session.execute(
+        select(Attempt).where(Attempt.token_hash == token_hash_hex)
+    )
+    return res.scalar_one_or_none()
 
-async def list_answers_by_attempt_id(session: AsyncSession, attempt_id) -> Sequence[Answer]:
-    """
-    Retorna respostas ordenadas por question_number (1..100).
-    """
+
+async def get_attempt_by_token(session: AsyncSession, token: str) -> Optional[Attempt]:
+    from backend.services.token import token_hash
+
+    th = token_hash(token)
+    return await get_attempt_by_token_hash(session, th)
+
+
+async def list_answers_by_attempt_id(session: AsyncSession, attempt_id) -> list[Answer]:
     res = await session.execute(
         select(Answer)
         .where(Answer.attempt_id == attempt_id)
         .order_by(Answer.question_number.asc())
     )
-    return res.scalars().all()
-
-
-async def count_answers_by_attempt_id(session: AsyncSession, attempt_id) -> int:
-    res = await session.execute(select(func.count()).select_from(Answer).where(Answer.attempt_id == attempt_id))
-    return int(res.scalar_one() or 0)
+    return list(res.scalars().all())
 
 
 async def upsert_answer(
@@ -94,9 +107,6 @@ async def upsert_answer(
     choice: str,
     letter_scored: str,
 ) -> None:
-    """
-    Upsert idempotente por (attempt_id, question_number).
-    """
     res = await session.execute(
         select(Answer).where(
             Answer.attempt_id == attempt_id,
@@ -110,13 +120,20 @@ async def upsert_answer(
         session.add(existing)
         return
 
-    a = Answer(
+    ans = Answer(
         attempt_id=attempt_id,
         question_number=int(question_number),
         choice=choice,
         letter_scored=letter_scored,
     )
-    session.add(a)
+    session.add(ans)
+
+
+async def count_answers_by_attempt_id(session: AsyncSession, attempt_id) -> int:
+    res = await session.execute(
+        select(func.count(Answer.id)).where(Answer.attempt_id == attempt_id)
+    )
+    return int(res.scalar_one() or 0)
 
 
 def clamp_progress(answered_count: int) -> int:
@@ -137,13 +154,10 @@ async def recompute_progress(session: AsyncSession, attempt: Attempt) -> int:
     return int(attempt.progress or 0)
 
 
-# --- participant fields used by submit (SSOT) ---
-
 async def get_tipo_aplicacao_by_attempt(session: AsyncSession, attempt: Attempt) -> str:
     """
     Fonte: Participant.tipo_aplicacao (SSOT).
     """
-    # tenta relationship carregada
     try:
         part = getattr(attempt, "participant", None)
         if part is not None:
@@ -153,7 +167,9 @@ async def get_tipo_aplicacao_by_attempt(session: AsyncSession, attempt: Attempt)
     except Exception:
         pass
 
-    res = await session.execute(select(Participant.tipo_aplicacao).where(Participant.id == attempt.participant_id))
+    res = await session.execute(
+        select(Participant.tipo_aplicacao).where(Participant.id == attempt.participant_id)
+    )
     t = res.scalar_one_or_none()
     if isinstance(t, str) and t.strip():
         return t.strip().lower()
@@ -166,6 +182,7 @@ async def create_participant_and_attempt_from_invite(
     nome: str,
     sobrenome: Optional[str],
     email: Optional[str],
+    empresa_nome: Optional[str] = None,
     tipo_aplicacao: str,
     token_hash_hex: str,
     cliente_id,
@@ -175,11 +192,14 @@ async def create_participant_and_attempt_from_invite(
     invite_id,
     testdef_version: str = "v1",
 ) -> Attempt:
-    """Cria Participant + Attempt usando o contexto vindo do Invite."""
+    """
+    Cria Participant + Attempt usando o contexto vindo do Invite.
+    """
     p = Participant(
         nome=nome,
         sobrenome=sobrenome,
         email=email,
+        empresa_nome=empresa_nome,
         tipo_aplicacao=tipo_aplicacao,
     )
     session.add(p)
diff --git a/backend/services/invite_flow.py b/backend/services/invite_flow.py
index 846f969..550390a 100644
--- a/backend/services/invite_flow.py
+++ b/backend/services/invite_flow.py
@@ -22,37 +22,39 @@ async def start_attempt_from_invite(
     nome: str,
     sobrenome: Optional[str],
     email: Optional[str],
+    empresa_nome: Optional[str] = None,
 ) -> InviteStartResult:
     """
     Fluxo público do invite:
     - valida Invite via token_hash(token)
     - cria Participant + Attempt usando cliente/rodada/setor/cargo/tipo_aplicacao do Invite
-    - marca Invite como USED (com attempt_id)
-    - redireciona para /p/{token}/q/1 (reusa o mesmo token do invite)
+    - marca Invite como OPENED (com attempt_id)
+    - redireciona para /t/{token}/q/1 (reusa o mesmo token do invite)
     """
     th = token_hash(token)
     inv = await repo_invite.get_invite_by_token_hash(session, th)
-    if not inv or inv.status in ("CANCELLED","EXPIRED","COMPLETED"):
+    if not inv or inv.status in ("CANCELLED", "EXPIRED", "COMPLETED"):
         raise ValueError("Invalid invite")
 
-    # se já usado e tem attempt_id, só seguir
-    if inv.status in ("OPENED","COMPLETED") and inv.attempt_id:
+    # Se já usado e tem attempt_id, só seguir
+    if inv.status in ("OPENED", "COMPLETED") and inv.attempt_id:
         return InviteStartResult(token=token)
 
-    # se já existe attempt com este token_hash, só seguir e sincronizar invite -> USED
+    # Se já existe attempt com este token_hash, só seguir e sincronizar invite -> OPENED
     existing = await repo_attempt.get_attempt_by_token_hash(session, th)
     if existing:
         await repo_invite.mark_invite_opened(session, invite_id=inv.id, attempt_id=existing.id)
         await session.commit()
         return InviteStartResult(token=token)
 
-    # criar Participant + Attempt (sem usar seed default; usa dados do invite)
+    # Criar Participant + Attempt usando contexto do invite
     attempt = await repo_attempt.create_participant_and_attempt_from_invite(
         session,
         invite_id=inv.id,
         nome=nome,
         sobrenome=sobrenome,
         email=email,
+        empresa_nome=empresa_nome,
         tipo_aplicacao=inv.tipo_aplicacao,
         token_hash_hex=th,
         cliente_id=inv.cliente_id,
diff --git a/backend/services/public_flow.py b/backend/services/public_flow.py
index 3a08fcd..863b361 100644
--- a/backend/services/public_flow.py
+++ b/backend/services/public_flow.py
@@ -22,6 +22,7 @@ async def start_public_attempt(
     nome: str,
     sobrenome: Optional[str],
     email: Optional[str],
+    empresa_nome: Optional[str] = None,
     tipo_aplicacao: str,
     testdef_version: str = "v1",
 ) -> StartResult:
@@ -41,6 +42,7 @@ async def start_public_attempt(
         nome=nome,
         sobrenome=sobrenome,
         email=email,
+        empresa_nome=empresa_nome,
         tipo_aplicacao=tipo_aplicacao,
         token_hash_hex=th,
         testdef_version=testdef_version,
